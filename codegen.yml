overwrite: true
schema: "src/generated/githubAPI-schema-loader.js"
documents:
    - src/graphql/queries/*.graphql
    - src/graphql/mutations/*.graphql
generates:
    src/generated/graphql.tsx:
        plugins:
            - "typescript"
            - "typescript-operations"
            - "typescript-react-apollo"

