directive @client on FIELD # Обязательно, для того чтобы IDE понимала эту директиву в @client запросах!

# type Login {
#     name: String!
#     date: String!
# }

extend type Query {
    isLoggedIn: Boolean!
}

